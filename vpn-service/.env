# VPN Service Environment Configuration
# Cleaned version - only essential variables kept
# App settings moved to database via admin panel

# System Settings
DEBUG=false
ENVIRONMENT=production

# Database Configuration  
DATABASE_URL=postgresql+asyncpg://vpn_user:vpn_password@db:5432/vpn_db

# Security (критически важные настройки - остаются в ENV)
SECRET_KEY=vpn_bezlagov_secure_key_2024_production
ALGORITHM=HS256

# Payment Provider Secrets (безопасность - остаются в ENV)
ROBOKASSA_SHOP_ID=bezlagov
ROBOKASSA_PASSWORD1=KXS28SKL9y5V9NVQiRSG
ROBOKASSA_PASSWORD2=ITixW86d0piZmf7FB1KV
ROBOKASSA_TEST_MODE=false
ROBOKASSA_HASH_TYPE=MD5

# X3UI Compatibility (legacy support)
X3UI_USERNAME=admin
X3UI_DEFAULT_INBOUND_ID=2

# Infrastructure
BACKEND_URL=http://backend:8000
LOG_LEVEL=INFO

# MOVED TO DATABASE (via admin panel):
# - APP_NAME → app_settings.site_name
# - TELEGRAM_BOT_TOKEN → app_settings.telegram_bot_token  
# - ADMIN_TELEGRAM_IDS → app_settings.admin_telegram_ids
# - ADMIN_USERNAMES → app_settings.admin_usernames
# - ACCESS_TOKEN_EXPIRE_MINUTES → app_settings.token_expire_minutes

# REMOVED (unused/empty variables):
# - POSTGRES_DB/USER/PASSWORD (duplicated DATABASE_URL)
# - TELEGRAM_WEBHOOK_URL (empty)
# - YOOKASSA_* (empty - for future)
# - COINGATE_* (not used)
# - REDIS_URL (Redis not connected)
# - SENTRY_DSN (empty) 